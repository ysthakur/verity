package com;

//import java.lang.String;
import java.lang.*;

class Test {
  int foobar = 5;

  public Test(int foo) {
    int bar = foo;
    given String xyzzy = "2567";
    boolean baz = isEven(bar);
    Test blah = new Test(bar);
    String foo = "foo";
  }

  public boolean isEven(int x)(given String foo) {
    return x % 2 < 0; //operators aren't checked yet
  }

  public static void main() {
    // System.out.println("Hello world!");
  }

  private static void foo(int bar);
}
